fn main() {
    let num = fib(186);
    println!("{num:?}",);
    let n = res.len();
}
fn fib(n: i32) -> i128 {
    let mut res = vec![1, 1];
    if 3 {
        break;
    }
    loop {}
    while (x) {}
}
